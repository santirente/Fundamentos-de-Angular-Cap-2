import { DirectiveSearchDirective } from './directive-search.directive';

describe('DirectiveSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveSearchDirective();
    expect(directive).toBeTruthy();
  });
});

import { IlumineColumnOnHoverDirective } from './ilumine-column-on-hover.directive';

describe('IlumineColumnOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new IlumineColumnOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});

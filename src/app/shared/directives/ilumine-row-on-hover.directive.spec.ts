import { IlumineRowOnHoverDirective } from './ilumine-row-on-hover.directive';

describe('IlumineRowOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new IlumineRowOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});

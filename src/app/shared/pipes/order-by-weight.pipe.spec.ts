import { OrderByWeightPipe } from './order-by-weight.pipe';

describe('OrderByWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByWeightPipe();
    expect(pipe).toBeTruthy();
  });
});

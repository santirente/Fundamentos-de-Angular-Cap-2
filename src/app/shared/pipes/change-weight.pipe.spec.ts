import { ChangeWeightPipe } from './change-weight.pipe';

describe('ChangeWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeWeightPipe();
    expect(pipe).toBeTruthy();
  });
});

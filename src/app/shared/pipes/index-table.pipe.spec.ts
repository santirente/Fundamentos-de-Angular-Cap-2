import { IndexTablePipe } from './index-table.pipe';

describe('IndexTablePipe', () => {
  it('create an instance', () => {
    const pipe = new IndexTablePipe();
    expect(pipe).toBeTruthy();
  });
});
